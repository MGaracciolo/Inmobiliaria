@model IEnumerable<net.Models.UsoInmueble>
@{
	ViewData["Title"] = "Usos";
}

<div class="text-center">
	<h3 class="display-4">Usos registrados:</h3>
</div>
<table class="table row-border dt-center" id="usosTable">
	<thead >
		<tr>
			<th>Uso</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>@Html.DisplayFor(modelItem => item.UsoValor)</td>
				<td>
					<a href="#" data-url="/UsoInmueble/Baja/@item.UsoId" class="text-danger desactivar"><i class="fa-solid fa-trash"></i></a>
				</td>
			</tr>
		}
	</tbody>
	<tfoot>
		<tr>
			<td colspan="2">
				<a href="#" data-bs-toggle="modal" data-bs-target="#modal_uso"> <i class="fa-solid fa-plus"></i></a>
			</td>
		</tr>
	</tfoot>
</table>

<div class="modal fade" tabindex="-1" role="dialog" id="modal_uso">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Nuevo uso:</h5> 
                <button type="button" class="close" data-bs-dismiss="modal" aria-label="Cerrar">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form asp-controller="UsoInmueble" asp-action="Guardar" method="post">
                    <div class="form-group">
                        <label for="UsoValor" class="control-label">Valor:</label>
                        <input type="text" id="UsoValor" name="UsoValor" class="form-control" />
                        <span class="text-danger" ></span>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Guardar" class="btn btn-primary" />
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>


@section Scripts {
	<script>
		$(document).ready(function () {
			$('.desactivar').click(function (e) {
				e.preventDefault();
				var url = $(this).data('url');
				Swal.fire({
					title: "¿Eliminar el tipo de uso?",
					text: "El dato se elimina definitivamente",
					icon: "warning",
					showCancelButton: true,
					confirmButtonColor: "#3085d6",
					cancelButtonColor: "#d33",
					confirmButtonText: "Desactivar",
					cancelButtonText: "Cancelar"
				}).then((result) => {
					if (result.isConfirmed) {
						window.location.href = url;
					}
				});
			});
		});
	</script>
}